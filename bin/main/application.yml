server:
  port: 8888
  
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/KR_TRAINING
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true